---
title: Radicle blog
---

# A blog about human interface design

{% for post in site.posts %}
  <article>
    <div card-background></div>
    <h1 no-margin>
      <span descriptor>
        {{ post.type | default: "blog post" }}<span style="float:right">{{ post.date | date: "%e %b %Y" }}</span>
      </span>
      <a href="{{ post.url }}">
        {{ post.title }}
      </a>
    </h1>
    <p without-vertical-margins lighter>{{ post.excerpt }}</p>
  </article>
{% endfor %}

<hr asterism>
